import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 20,
    marginBottom: 10,
  },
  part: {
    flex: 1,
  },
  partCount: {
    fontWeight: 'bold',
    fontSize: 18,
  },
  partText: {
    fontSize: 16,
  },
});

export default styles;
